function CentimeterToMeter(CM) {
    const meter = CM *0.01;
    return meter
}

const CentiMeter = 2356;
const ToMiter = CentimeterToMeter(CentiMeter);
console.log(CentiMeter ,'Centimeter are', ToMiter, 'Miters');